# Audit codebase — install with:  pip install -r requirements.txt
# Pinned to the versions used to produce the numbers in the Comment.
numpy==1.23.5
pandas==1.5.3
scikit-learn==1.3.2
xgboost==2.1.4
shap==0.44.1
matplotlib==3.7.5
pyarrow==17.0.0
joblib==1.4.2

# To open and run audit_leakage.ipynb:
jupyter==1.0.0
ipykernel==7.3.0

# Newer patch releases of the above generally reproduce the same results to
# within rounding; the pins guarantee bit-for-bit agreement with the manuscript.
